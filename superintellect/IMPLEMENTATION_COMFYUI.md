# 🛠️ ComfyUI для M1 Mac: Повне встановлення та оптимізація

Ця інструкція призначена для локального запуску важких AI-моделей (FLUX.2, SVD, Wan2.1) на процесорах Apple Silicon.

## 1. Підготовка (Prerequisites)
- **macOS**: 13.0+ (рекомендується 14.0 Sonoma або новіша для кращої підтримки Metal).
- **Homebrew**: Повинен бути встановлений.
- **Python**: Рекомендується версія 3.11.

## 2. Встановлення (Installation)

Виконай ці команди в терміналі:

```bash
# Клонуємо репозиторій
git clone https://github.com/comfyanonymous/ComfyUI
cd ComfyUI

# Створюємо віртуальне середовище
python3 -m venv venv
source venv/bin/activate

# Встановлюємо PyTorch для Apple Silicon
pip3 install --pre torch torchvision torchaudio --extra-index-url https://download.pytorch.org/whl/nightly/cpu

# Встановлюємо залежності
pip install -r requirements.txt
```

## 3. Запуск та Оптимізація (Crucial!)
Для Apple Silicon (M1/M2/M3/M4) запускай ComfyUI тільки з цими флагами:

```bash
python main.py --force-fp16 --use-split-cross-attention
```

### Що дають ці флаги:
- `--force-fp16`: Зменшує споживання VRAM вдвічі. Без цього FLUX.2 може не запуститися на 8GB/16GB версіях M1.
- `--use-split-cross-attention`: Значно прискорює генерацію на архітектурі Metal.

## 4. Додавання моделей (Models)
Завантаж моделі та поклади їх у відповідні папки:
1. **FLUX.2 [dev] або [schnell]**: `models/checkpoints/`
2. **SVD (Stable Video Diffusion)**: `models/checkpoints/`
3. **Upscalers (для покращення якості відео)**: `models/upscale_models/`

## 5. Очікувана продуктивність (M1)
- **Зображення (FLUX.2 1024x1024)**: ~1-3 хв.
- **Відео (SVD, 2 сек, 25 кадрів)**: ~30-60 хв.
- **Відео (Wan2.1 480p)**: ~1.5 - 2 години.

## 6. Лайфхак для відео
Замість того щоб генерувати довге відео одразу, згенеруй **1 ключовий кадр** у FLUX.2, а потім використовуй його як вхідний для **SVD (Image-to-Video)**. Це дасть найкращу якість за найменший час.

---
*Оновлено для Superintellect Series Production, 2026*
