# Інструкція з Деплою

## 1. Локальна збірка статичних файлів

Щоб створити статичну версію сайту:

```bash
npm run build
```

Це створить папку `.vitepress/dist` з усіма статичними HTML/CSS/JS файлами, готовими до публікації на будь-якому хостингу.

Щоб переглянути збудований сайт локально:

```bash
npm run preview
```

## 2. Автоматична публікація на GitHub Pages

### Крок 1: Налаштування репозиторію

1. Створи репозиторій на GitHub (наприклад, `willni`)
2. Запуш код:
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Will-n-i manifest"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/willni.git
   git push -u origin main
   ```

### Крок 2: Увімкнення GitHub Pages

1. Перейди в Settings репозиторію
2. Знайди розділ "Pages" в лівому меню
3. В "Build and deployment" обери:
   - **Source**: GitHub Actions
4. Збережи зміни

### Крок 3: Налаштування base path (якщо потрібно)

Якщо твій репозиторій НЕ називається `username.github.io`, а має назву типу `willni`, то:

1. Відкрий `.vitepress/config.mjs`
2. Розкоментуй рядок:
   ```javascript
   base: '/willni/',
   ```
   (замість `willni` підстав назву свого репозиторію)

### Крок 4: Автоматичний деплой

Тепер при кожному `git push` в гілку `main`:
1. GitHub Actions автоматично запустить workflow
2. Збудує статичний сайт
3. Опублікує на GitHub Pages

Твій сайт буде доступний за адресою:
- `https://YOUR_USERNAME.github.io/willni/` (якщо base встановлено)
- або `https://YOUR_USERNAME.github.io/` (якщо репозиторій `username.github.io`)

## 3. Альтернативні варіанти хостингу

### Netlify
1. Підключи репозиторій до Netlify
2. Build command: `npm run build`
3. Publish directory: `.vitepress/dist`

### Vercel
1. Підключи репозиторій до Vercel
2. Framework Preset: VitePress
3. Автоматично визначить команди

### Cloudflare Pages
1. Підключи репозиторій
2. Build command: `npm run build`
3. Build output directory: `.vitepress/dist`

## 4. Вічне збереження (IPFS)

Після збірки (`npm run build`):

```bash
# Встанови IPFS Desktop або CLI
ipfs add -r .vitepress/dist

# Отримаєш CID, наприклад: QmXyZ...
# Доступ: https://ipfs.io/ipfs/QmXyZ...
```

Для автоматичного пінінгу використовуй сервіси:
- Pinata.cloud
- Web3.Storage
- Fleek.co

---

**Головне**: Після першого push в `main`, GitHub Actions автоматично все зробить за тебе! 🚀
